# LeaUI v2.1 — Projects: Backend Architecture & Decisions

> Status: **living doc.** Source of truth for the **v2.1 Projects** backend +
> data design. Continues the numbering of [`v2-architecture.md`](./v2-architecture.md)
> (which ends at D20) — Projects decisions are **D21+**, so references never
> collide. v2 core is built & green; this is the next chapter. UI lives in
> [`v2-ui-design.md`](./v2-ui-design.md) §3.4 / [`v2-mockup.html`](./v2-mockup.html)
> (the project window). Backend comes first, as in v2.
>
> **Designed from first principles.** This is **not** a port of the older "project"
> feature. The legacy `prover/lea/project.py` and the parked `adapter/_deferred/project_*.py`
> are treated as **prior art to study, not code to reuse** — where the clean design
> happens to land near them, fine; where it doesn't, we build fresh. No monkey-patching,
> no bending the design to fit old code.
>
> **How to read this:** §0 is the canonical current design (read first). §1+ are the
> numbered decisions D21–D32 with rationale + rejected alternatives (the *why*). §9 is
> the file layout; §10 the build plan (→ seeds `v2.1-backend-progress.html`).

---

## 0. Current architecture at a glance

A **project** is a first-class shared workspace — a directory + git repo + DB index that
owns Instructions, Memory, Files, a **blueprint** (the lemma breakdown + dependency graph),
and a set of sessions. The design extends v2's invariants (filesystem-canonical D3, git
owns content D7/D8, DB is an index D4, stateless adapter-authoritative `run` D16, typed
events D17) to a shared, multi-session workspace. (Decision refs in parentheses.)

- **"Project" is an adapter-only concept; the prover stays project-agnostic** (D32). The
  prover already takes `messages` (D16, messages-in) and a `working_dir`; that is all a
  project needs. Project context (goal, instructions, memory, blueprint, files) is just a
  **composed message** the adapter prepends; the working dir is the project repo. The
  legacy prover-side `project.py` (which makes the *prover* regex-parse and write a markdown
  index — prover-side state, violating D16) is **deleted**, not reused.
- **A project = one shared directory + one git repo + one DB index row** (D21). The
  directory **is** the project; `rm -rf` deletes it; its `git log` is the project's whole
  history (one repo for proofs + instructions + memory + files → project-scoped
  git-as-memory, resolving v2 D7 drawback #2).
- **The layout is dictated by Lake, not by legacy** (D22). The lakefile declares
  `lean_lib Lea { srcDir := "proofs" }`, so a module `Lea.<Project>.Foo` *must* live at
  `proofs/Lea/<Project>/Foo.lean` to be importable. Lemmas-that-chain (the user's
  "function-chaining") is therefore a hard requirement that **forces** project proofs to sit
  at `proofs/Lea/<Project>/` (directly under the srcDir — unlike loose sessions at
  `proofs/<uuid>/…`, which are isolated and non-importable on purpose). Non-Lean project
  assets live in a Lake-invisible `.lea/` subfolder in the same per-project repo.
- **Project sessions write into the project repo from turn 1** (D23) — they share the dir
  and namespace, so a new proof can `import`/`open` an already-proved sibling immediately.
  Loose sessions are unchanged. A session belongs to at most one project.
- **The git store resolves the repo from the session's project** (D24): project session →
  the project repo; loose → its own `proofs/<uuid>/` repo. Every git primitive
  (commit-every-write, `git show` snapshot, `diff`, `uncommitted_diff`) is reused unchanged.
- **Instructions + Memory + blueprint + file inventory are composed by the adapter into one
  context message** (D25, D26) and prepended to the run's `messages`. DB-canonical; mirrored
  to `.lea/*.md` for human/git visibility. No implicit `lea.md`, no prover project code.
- **Files/uploads** (D27) live in `.lea/files/`, are indexed in `project_files`, and are
  text-extracted by tier (native text → pdf/docx → images deferred). The agent reads them
  via the **existing `read_file` tool** (they're in its working dir) + an inventory line in
  context — no RAG in v2.1.
- **The visualization is a blueprint dependency graph** (D28, D29), modeled on Lean's
  `leanblueprint` dep-graph: nodes are the **planned** breakdown (definitions + lemmas +
  theorems, *including the ones not done yet*), edges are `uses` dependencies, **shape
  encodes kind** (box = definition, ellipse = lemma/theorem) and **color encodes live
  status** (green = proved, blue = statement formalized / ready to prove, white = planned).
  Planned nodes are stored (`project_nodes`); status + the proved-subset are **derived** from
  the live Lean state. The proved subset **is** the "reusable lemmas" half of Memory — one
  model, not two. Recording-a-proof-against-a-node happens **adapter-side** on `Finished`
  (D29), keeping the prover stateless.

**Build status:** design only — nothing built. v2 core (adapter 91 · prover 77) is the
foundation. Build plan in §10; live status in `v2.1-backend-progress.html` (to be created
once these decisions are reviewed).

---

## 1. What a project is

### D21 — A project is a shared directory + git repo + DB index row (not a filter)
A project owns one on-disk directory that is **its own git repo**, plus a `projects` row
indexing it. Its sessions, proofs, instructions, memory, files, and blueprint all live under
that directory / reference that row. **It is an adapter-side concept end to end** (D32).
- **Why:** the user's framing is "all of this in a single shared project directory." One
  dir = the project: `git log` there is the full history, `rm -rf` deletes it, and a shared
  dir is what lets proofs reference each other (D22). Mirrors v2's "the dir *is* the session"
  (D7), scaled to "the dir *is* the project."
- **Rejected:** *project = a DB tag/filter over loose sessions.* Gives a sidebar grouping but
  no shared directory, shared namespace, cross-proof reuse, project memory, or blueprint —
  i.e. none of the feature. The shared workspace is the whole value.
- **Consequence:** `sessions.project_id` (`NULL` = loose) selects loose vs in-project behavior.

---

## 2. Filesystem & git layout

### D22 — The layout is dictated by Lake's module resolution
The workspace lakefile declares `lean_lib Lea where srcDir := "proofs"`. Lean resolves a
module `Lea.<Project>.Foo` to the file `proofs/Lea/<Project>/Foo.lean`. **For project lemmas
to chain (import each other) they must therefore live directly under the srcDir as
`proofs/Lea/<Project>/`** — there is no other Lake-valid location. Loose sessions sit at
`proofs/<uuid>/Lea/Misc/…`, whose `<uuid>` segment makes them deliberately *non-importable*
(isolated standalone files); projects are the opposite by requirement.

So a project directory + git repo is:
```
proofs/Lea/<Project>/
  Foo.lean, Bar.lean, …      ← the project's proofs (importable as Lea.<Project>.* → chaining)
  .lea/                      ← Lake-invisible project assets (non-.lean; Lake globs only .lean)
    instructions.md          ← mirror of DB Instructions (D25)
    memory.md                ← mirror of DB Memory (D26)
    blueprint.json|md        ← mirror of the DB blueprint nodes/edges (D28)
    files/                   ← uploaded docs + extracted text (D27)
  .git/                      ← the project repo
```
- **Why `proofs/Lea/<Project>/`:** it is the *only* path that satisfies the chaining
  requirement under the given lakefile — a first-principles constraint, derived here, not
  inherited. `<Project>` is a valid Lean module-name segment (UpperCamel, no dashes); the
  adapter derives it from the project slug.
- **Why `.lea/` inside the same repo:** keeps "single shared directory" literal and puts
  instructions/memory/blueprint/files under the **same `git log`** as the proofs. The
  dot-prefix + non-`.lean` contents guarantee Lake never treats it as a module.
- **Rejected:** assets in a sibling `workspace/projects/<slug>/` tree (splits the project
  across two roots + two repos); a per-project Lake *library* with its own srcDir (needs
  lakefile edits per project — pointless complexity when one `Lea` lib already namespaces
  cleanly).
- **Drawback (logged):** the slug → `<Project>` namespace → dir path chain means a slug
  rename is a git-mv + import rewrite. **Slugs are immutable** in v2.1 (rename = title only).

### D23 — Project sessions write into the project repo from turn 1 (no prove-then-promote)
Creating a session inside a project sets its working dir to the project repo; the agent
writes under `Lea/<Project>/…` in the project namespace from the first turn, able to import
sibling lemmas live. Loose sessions are unchanged (own `proofs/<uuid>/` repo, `Lea.Misc`).
- **Why:** matches "sessions start inside the project and share the directory." Proofs
  reference each other immediately; the module name is real from the start; no rewrite step.
- **Rejected — prove-loose-then-promote:** prove in `Lea.Misc`, then move the file into the
  project and rewrite its namespace. The move is **cross-repo** (loose repo → project repo),
  so git history doesn't follow; the module name is throwaway until promotion; and it
  contradicts shared-from-the-start. (This was the legacy `_deferred` workflow; we don't
  revive it. A "move a loose session into a project" convenience may exist later, but built
  clean, and never as the primary path.)
- **Concurrency:** many sessions share one repo, but v2's global one-run lock
  (`active_run_lock`) serializes all writes app-wide, so per-project contention is moot today
  (Open Q-5).

### D24 — The git store resolves the repo from the session's project
v2's `GitStore` keys operations on a repo path derived from `session_id`. Generalize the
resolution: **project session → the project repo (`proofs/Lea/<Project>/`); loose → its own
repo.** A single `repo_for_session(session) -> Path` resolver (fed `project_id` + the
project's `<Project>`) is called by the bridge and the file/check/verify routes; every
primitive (`commit_write`, `snapshot`, `diff`, `head`, `uncommitted_diff`) operates on the
resolved path, unchanged.
- **Per-session vs project history:** the per-session view is `code_steps` filtered by
  `session_id` (the DB is the per-session index); the project repo's `git log` is the
  project-wide, interleaved view (intended).
- **Why:** the repo boundary is the *only* thing differing between loose and project work;
  isolating it in one resolver keeps the bridge and routes project-agnostic.

---

## 3. Context the agent receives — Instructions & Memory

### D25 — Project context is one composed message the adapter prepends (not prover code, not `lea.md`)
`run_events` is messages-in (D16/A9). So project context — the goal/Instructions, Memory,
the blueprint (what's proved / what's planned / how it chains), and the file inventory — is
assembled by the adapter into **one context message** prepended to the run's `messages`.
Instructions are **DB-canonical** (a `projects` column), editable via the API, and mirrored
to `.lea/instructions.md` for human/git visibility.
- **Why:** it's the cleanest possible seam — no new prover surface, no implicit file
  discovery, deterministic and per-run. It honors D16 (adapter composes the input; prover is
  pure compute) and D32 (prover knows nothing about "projects").
- **Rejected:** the implicit `lea.md` discovery in `prompt.py` (order-dependent, global, not
  per-project); the legacy `project.py` `project_context_message` + markdown-marker parsing
  (prover-side, brittle regex, violates D16). Both are replaced by "compose a message."
- **Namespace/location guidance:** the composed message states the write location
  (`proofs/Lea/<Project>/`) and namespace (`Lea.<Project>`) explicitly. The system prompt's
  hardcoded `Lea.Misc` workspace block is **parameterized** (a small, clean prover-prompt
  change — D32) so it states the active namespace instead of always `Lea.Misc`.

### D26 — Memory = DB items (free-form) + the derived proved-subset of the blueprint
Two halves, matching the mockup:
- **User/free-form items** (preferences, hints, "try splitting at x=1"): a `project_memory`
  table — `id, project_id, kind ('preference'|'fact'|'note'), content, source ('user'|'agent'),
  created_at`. User adds/edits/deletes via the API.
- **The reusable-lemma registry** (what's proved + signatures + how to import it): **not a
  second store** — it is the **proved-status subset of the blueprint nodes** (D28). Derived,
  always truthful (a node is `proved` only when its Lean decl passes), nothing to hand-maintain.

Both halves are rendered into the composed context message (D25).
- **Why derive the registry:** a separate "what's proved" list would drift from git + the
  blueprint (same derive-don't-store stance as v2 C3/C4). The blueprint already holds it.
- **Scope:** v2.1 = user-added memory + derived registry. *Agent autonomously writing
  free-form memory* (e.g. failure post-mortems) is **deferred** — needs a memory-write tool +
  a truthfulness policy; failure signal can be derived from failed sessions later (Open Q-1).

---

## 4. Files & uploads

### D27 — Files in `.lea/files/`, indexed in DB, tiered text extraction, read via `read_file`
Uploaded docs (`.pdf/.tex/.docx/images`) are saved to `.lea/files/` (versioned in the project
repo) and indexed in `project_files` — `id, project_id, filename, stored_path, mime,
kind ('upload'|'blueprint'|'extract'), extracted_path, created_at`. Extraction is **tiered**:
- **Tier 1 — native text** (`.tex/.md/.txt`): used directly.
- **Tier 2 — extractable** (`.pdf/.docx`): extracted to `.lea/files/<name>.txt` at upload
  (pdfminer.six / python-docx).
- **Tier 3 — images** (`.png/.jpg`): stored + downloadable, **not** extracted (vision/OCR is a
  later, model-side concern).

The agent reads extracted text through the **existing `read_file` tool** (the files are in its
working dir); the composed context (D25) carries a one-line **inventory**. No embeddings/RAG.
- **Why phased + read-file, not RAG:** simplest thing that works. Native text is free; pdf/docx
  are the common math sources; vision deferred. On-demand `read_file` respects the context
  budget and reuses an existing tool — no retrieval path until file volume forces one (Open Q-2).
- **Guards:** per-file size cap + MIME allowlist at the upload endpoint (no executables/scripts).

---

## 5. The blueprint & dependency graph (the visualization)

### D28 — The graph is a *blueprint*: planned nodes + `uses` edges, shape = kind, color = live status
Inspired by Lean's `leanblueprint` dep-graph (the PFR-style graph the user pointed at) — taken
as **inspiration, not a spec to match exactly.** A project has a **blueprint**: the planned
decomposition of the goal into nodes. Each node:
```
key          stable handle (e.g. 'ruzsa-triangle')
kind         'definition' | 'lemma' | 'theorem'          → node SHAPE (box | ellipse)
title        short human label
statement    NL / LaTeX statement (optional until formalized)
lean_name    the Lean declaration name (NULL until formalized)
uses         [key, …]  — dependency edges (the chaining, declared)
session_id   the session working/that proved it (NULL until claimed)
```
- **Status is derived, not stored** (D29) → node **color**: `planned` (white, no Lean yet) ·
  `stated` (decl exists, proof has `sorry`/incomplete) · `proved` (`lean_check` ok, no `sorry`;
  SafeVerify-confirmed where available, green) · `failed` (last attempt errored). A `ready`
  flavor (blue — all deps proved, ready to attempt) is derived from the edges.
- **Why a blueprint, not "derive the graph from finished proofs":** the value is showing
  *what isn't done yet* and how the pieces will connect — "understand what the agent is trying
  to accomplish." A purely after-the-fact graph can't show the plan. The blueprint is also the
  agent's task decomposition: it's rendered into the context (D25) so the agent knows which
  proved lemmas to reuse and which node it's attacking.
- **Edges (`uses`) are declared on the node** (so the planned graph exists before any Lean),
  and may be **corroborated** by scanning the proved Lean for actual import/name references
  (a clean, fresh detector — *not* the legacy `project_usage.py`, studied only as prior art).
- **Exposed as** `GET /api/projects/{id}/graph -> {nodes, edges}` with kind + derived status
  per node; the frontend renders shapes/colors. Same scan feeds Memory's registry (D26).
- **Rejected:** persisting status/edges as cached columns (must be invalidated on every write
  — drift for no gain at this scale); full leanblueprint LaTeX-compat parsing (heavy; we store
  structured nodes instead, with LaTeX only as the optional `statement` body — Open Q-3).

### D29 — Node sourcing, status derivation, and adapter-side recording on success
- **Sourcing:** blueprint nodes are authored — by the user, or by the agent during the
  search/tex planning phase (it proposes a decomposition) — via the node API. A session
  *claims* a node (or creates one) when it starts working on it.
- **Status derivation:** computed on read from the live Lean state — the node's `lean_name`
  resolved in the project files, checked via the existing `check`/`verify` capabilities (v2 D2)
  + `sorry`/`axiom` scan. Never stored, so it can't lie.
- **Recording on success is adapter-side** (honoring D16/D32 — the prover writes *no* project
  state): when a project run emits `Finished`, the **adapter** derives the proved decl's
  `lean_name` + signature from the committed file, links it to the claimed/﻿matching node,
  and flips the node to `proved`. This replaces the legacy prover-side `record_project_entry`
  entirely — the source of truth is the DB blueprint + git, not a prover-written markdown file.

---

## 6. Database — schema deltas

### D30 — Schema deltas from the v2 `db.py` (clean rebuild, no migrations)
Continues v2's disposable-DB / create-table-authoritative stance (no `ALTER` migrations; a
schema change = fresh DB via `npm run reset:local`).

**`projects`** — extend:
```
id text pk · slug text unique (immutable; → namespace Lea.<Project>) · title · description
instructions text                 -- D25 (DB-canonical; mirrored to .lea/instructions.md)
namespace text not null           -- cached 'Lea.<Project>' (derivable; cached for queries)
repo_path text not null           -- 'proofs/Lea/<Project>' (the shared dir / repo, D22)
created_at · updated_at
```

**`project_nodes`** (new) — the blueprint (D28):
```
id · project_id (fk) · key (unique within project) · kind · title · statement
lean_name · uses (json array of keys) · session_id (fk, nullable) · created_at · updated_at
```
*(status is derived on read — D29 — not a column.)*

**`project_memory`** (new) — D26 free-form items:
```
id · project_id (fk) · kind ('preference'|'fact'|'note') · content · source ('user'|'agent') · created_at
```

**`project_files`** (new) — D27 uploads:
```
id · project_id (fk) · filename · stored_path · mime
kind ('upload'|'blueprint'|'extract') · extracted_path · created_at
```

**`sessions`** — `project_id` FK exists. Listing splits **loose** (`project_id IS NULL`, the
sidebar Chats group) vs **in-project** (the project window).

---

## 7. API surface (adapter)

### D31 — New/changed endpoints
```
GET    /api/projects                          list (title, slug, namespace, proof/node counts, status mix)
POST   /api/projects                          create {title, description?, instructions?} → slug, dir, repo
GET    /api/projects/{id}                      detail: meta + sessions + memory + files + blueprint
PUT    /api/projects/{id}                      edit title/description/instructions (re-mirror .lea/*.md)
DELETE /api/projects/{id}                      rm -rf the repo + cascade DB rows (confirm guard)

POST   /api/projects/{id}/sessions             create a session INSIDE the project (working dir = project repo, D23)

GET    /api/projects/{id}/blueprint            nodes + edges (authoring view)
POST   /api/projects/{id}/nodes                add a node {kind, title, statement?, uses?}
PUT    /api/projects/{id}/nodes/{key}          edit (incl. uses edges, lean_name, session claim)
DELETE /api/projects/{id}/nodes/{key}
GET    /api/projects/{id}/graph                derived render graph {nodes+kind+status, edges} (D28/D29)

GET    /api/projects/{id}/memory               list ·  POST add {kind, content} ·  DELETE /{mid}

POST   /api/projects/{id}/files                upload (multipart) → store + extract (D27)
GET    /api/projects/{id}/files  ·  GET …/files/{fid} (download)  ·  DELETE …/files/{fid}

GET    /api/search?q=                          global search across loose + project sessions, each
                                               tagged with its project. NEW: project sessions are
                                               hidden from the sidebar, so search is the only way to
                                               reach them. SQLite LIKE first; FTS5 later.
```
- **Run start** for a project session: the bridge resolves the project repo (D24), composes the
  context message (D25/D26 — instructions + memory + blueprint + file inventory), prepends it to
  `messages`, and runs. On `Finished` the adapter records the proved node (D29).
- **Existing run SSE** (`assistant_delta`/`message`/`code_step`/`status`/`done` + per-tool
  approval) is **reused unchanged**; `code_step` commits just land in the project repo (D24).

---

## 8. Prover-side: stays project-agnostic

### D32 — Delete the prover's project machinery; the prover knows nothing about "projects"
A project needs only two things the prover **already** provides: messages-in (D16/A9) and a
`working_dir` (threaded by the bridge in v2). So:
- **Delete `prover/lea/project.py`** (`ProjectContext`, `project_context_message`,
  `record_project_entry`, the markdown-marker parsing) and the **`project=` parameter** of
  `run_events`. They make the prover hold/produce project state (a prover-written markdown
  index, parsed by regex) — a direct violation of v2 D16 (adapter is the single source of truth;
  prover is pure compute) and the reason the legacy seam was brittle.
- **One small, clean prover change:** parameterize the system-prompt workspace/namespace block
  (currently hardcoded to `Lea.Misc`) so the adapter can state the active namespace + write
  location. This is a clean parameter, not a workaround.
- **Everything else is adapter-side:** context composition (D25), result recording (D29),
  blueprint/graph (D28), git (D24). The prover compiles and proves; the adapter owns "project."
- **Why this is cleaner, not just different:** it removes ~390 lines of prover-side
  regex/markdown state, makes the prover's contract uniform (loose and project runs differ only
  in the messages + working dir the adapter hands it), and keeps the D16 boundary honest.

---

## 9. File layout

```
adapter/app/
  routes/projects.py   🔜 CRUD + sessions-in-project + nodes/blueprint/graph + memory + files + search (D31)
  projects.py          🔜 project service: create dir/repo/.lea; compose context message; mirror
                       🔜   instructions/memory/blueprint .md; repo_for_session resolver (D21–D26)
  blueprint.py         🔜 node model, derived status (via check/verify), uses-edge scan, graph build (D28/D29)
  uploads.py           🔜 store + tiered text extraction (D27)
  gitstore.py          ✏️ generalize: repo resolves from the session's project (D24)
  bridge.py            ✏️ on project run: resolve repo + compose+prepend context message + record node (D24/D25/D29)
  db.py / store.py     ✏️ projects deltas + project_nodes/_memory/_files + loose/in-project listing + search (D30)

prover/lea/
  project.py           🗑️ DELETED — prover stays project-agnostic (D32)
  agent.py             ✏️ remove the `project=` param from run_events (D32)
  prompt.py            ✏️ parameterize the workspace/namespace block (D32)

proofs/Lea/<Project>/  🔜 per-project shared repo (proofs + .lea/) — runtime data (D22)
adapter/_deferred/     📚 prior art only — studied, NOT imported or revived (D23/D28)
```

---

## 10. Build plan (→ seeds `v2.1-backend-progress.html`)

One todo at a time · plan → your input → build + tests → tested → done (the v2 rhythm).

- **Group P — Project core**
  - P1 Schema deltas + store queries (`projects` fields, `project_nodes`/`_memory`/`_files`,
    loose/in-project listing) (D30)
  - P2 Project service: create `proofs/Lea/<Project>/` + git repo + `.lea/`; `repo_for_session`;
    generalize `gitstore` (D21/D22/D24)
  - P3 Project CRUD routes + delete-with-confirm (D31)
- **Group Q — Prover goes project-agnostic + sessions in a project**
  - Q1 Delete `project.py` + the `project=` param; parameterize the prompt namespace block (D32)
  - Q2 Create-session-in-project; bridge resolves the project repo + composes & prepends the
    context message (instructions+memory+blueprint+files) (D23/D24/D25)
  - Q3 Adapter-side record-on-`Finished`: link proved decl → node, flip status (D29) + real run smoke
- **Group R — Instructions & Memory**
  - R1 Instructions CRUD + mirror to `.lea/instructions.md` (D25)
  - R2 Memory CRUD + injection (D26)
- **Group S — Files**
  - S1 Upload endpoint + storage + MIME/size guards (D27)
  - S2 Tiered text extraction + inventory in context (D27)
- **Group T — Blueprint & graph**
  - T1 Node CRUD + `uses` edges + blueprint API (D28)
  - T2 Derived status (via check/verify + sorry scan) + uses-edge scan → `/graph` (D28/D29)
- **Group U — Search**
  - U1 Global search endpoint (loose + project-tagged) (D31)

---

## 11. Open questions (decide before/while building)

1. **Agent-written memory (D26):** v2.1 = user-add + derived registry only; let the agent
   autonomously append memory (failure notes) now, or defer? *Recommend defer.*
2. **File context delivery (D27):** read-file-on-demand + inventory (recommended) vs. stuffing
   extracted text vs. early RAG. *Recommend read-file; revisit when files exceed the budget.*
3. **Blueprint authoring format (D28):** structured nodes with optional LaTeX `statement`
   bodies (recommended) vs. importing a real leanblueprint `.tex`. *Recommend structured first;
   `.tex` import is a later bridge.*
4. **Graph granularity (D28):** nodes = lemmas/theorems (recommended; matches blueprint keys) vs.
   sessions. *Recommend lemma-level.*
5. **Concurrency in a shared repo (D23/D24):** the global one-run lock makes it moot today; if we
   ever allow concurrent runs, the project repo needs per-repo locking. *Logged; no v2.1 action.*
6. **`project.py` deletion timing (D32):** delete in Q1 (recommended — keeps the boundary honest
   from the start) vs. leave dormant until projects ship. *Recommend delete in Q1.*

---

## Decision index

D21 project = shared dir + git repo + DB index (adapter-only concept) ·
D22 layout dictated by Lake: project proofs at `proofs/Lea/<Project>/` (importable → chaining) +
Lake-invisible `.lea/` for assets, same repo ·
D23 project sessions write into the shared repo from turn 1 (reject prove-then-promote) ·
D24 git store resolves the repo from the session's project (project repo | loose per-session repo) ·
D25 project context = one composed message the adapter prepends (not prover code, not implicit
`lea.md`); Instructions DB-canonical ·
D26 Memory = DB free-form items + the *derived* proved-subset of the blueprint (one model) ·
D27 Files in `.lea/files/`, indexed, tiered text extraction, read via the existing `read_file`
tool (no RAG in v2.1) ·
D28 the visualization is a *blueprint* dependency graph (leanblueprint-inspired): stored planned
nodes (kind→shape) + `uses` edges, derived live status (→color) ·
D29 node sourcing (authored) + derived status (live Lean) + adapter-side recording on `Finished`
(no prover-written index) ·
D30 schema deltas: `projects` fields + `project_nodes` + `project_memory` + `project_files` +
loose/in-project listing (clean rebuild) ·
D31 API surface (project CRUD, sessions-in-project, blueprint/nodes/graph, memory, files, search) ·
D32 prover stays project-agnostic — delete `project.py` + the `project=` param; project is an
adapter-only concept; one clean prompt-namespace parameterization.
