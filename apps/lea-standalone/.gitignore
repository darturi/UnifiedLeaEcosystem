.DS_Store
node_modules/
dist/
.venv/
__pycache__/
.pytest_cache/
*.pyc
data/
config/lea.local.toml
# D6: per-role sub-agent overrides (local user data)
config/subagent-overrides.json

# Runtime dirs created by run-lea.sh / docker (persisted DB, settings, key)
lea-data/
lea-config/

# Local working notes / agent instructions — not part of the shipped repo
CLAUDE.md
TODO.md

